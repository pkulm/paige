$(document).ready(function() {
		
	jQuery('#camera_wrap_1').camera({
		thumbnails: false
	});
	
	
	/*
var $window = $(window);
        function checkWidth() {
        if ($window.width() < 767) {
            $('#title-column').removeClass('header40').addClass('header70');
            $('#nav-column').removeClass('header60').addClass('header30');
            };

        if ($window.width() >= 768) {
            $('#title-column').removeClass('header70').addClass('header40');
            $('#nav-column').removeClass('header30').addClass('header60');
        }
    }
    checkWidth();
    $(window).resize(checkWidth);
*/
});